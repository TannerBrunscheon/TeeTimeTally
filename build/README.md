# build

Contains Dockerfiles and deployment helpers for the project.

- `docker-compose.yaml` and `Dockerfiles/` are included for containerized development and deployments.
- `Railway/` contains deployment manifests for Railway (if used).
