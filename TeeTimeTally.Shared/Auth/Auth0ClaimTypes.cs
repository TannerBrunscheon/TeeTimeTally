namespace TeeTimeTally.Shared.Auth;

// constants for custom claim types that are relevant when working with Auth0.
public static class Auth0ClaimTypes
{
	public const string Scope = "permissions";
}
